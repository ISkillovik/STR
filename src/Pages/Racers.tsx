import styled from "styled-components";

const RacersMain = styled.div`
  height: 100vh;
`;

type Props = {};

const Racers = (props: Props) => {
  return <RacersMain>Racers</RacersMain>;
};

export default Racers;
